# W1 周报（8/12–8/16）· 首日进展

按《电气接口智能体-开发方案》执行。今日（8/12）完成 W1 主体工作：

## 已交付

| 交付物 | 位置 | 状态 |
|---|---|---|
| 仓库脚手架 | `D:\Downloads\IPT\`（目录结构见方案第二节） | ✅ |
| 数据库快照 | `data/eicd_snapshot_20260812.db`（186.8MB，sqlite backup API，WAL 安全） | ✅ |
| Schema 摸底 | `docs/schema摸底.md`（49 表全列结构+外键） | ✅ |
| 本体两件套 | `ontology/ontology.yaml`（22 类/28 关系，机器可读）+ `ontology.md`（含 mermaid 图） | ✅ |
| 脱敏管线 | `etl/anonymize.py` + `anonymize_config.yaml`（口径配置化）+ `verify_anonymize.py`（残留验证） | ✅ 286 探针全库扫描**零残留** |
| 脱敏库 | `data/eicd_anon.db`（123MB） | ✅ |
| 图谱 ETL | `etl/build_graph.py`（确定性可重跑，输出前悬挂边校验） | ✅ |
| 图谱 v0 | `graph/nodes.jsonl`(60MB) + `edges.jsonl`(29MB) + `stats.md`：**210,640 节点 / 401,938 关系**，24 类节点 / 32 类关系 | ✅ |
| 评测生成器骨架 | `eval/generate_eval.py`（4 类模板 × SQL 金标）→ 20 条样题 | ✅ |
| 正确性抽查 | 图谱文件独立复算 vs SQL 金标：**5/5 通过** | ✅ |

## 脱敏迭代记录（重要，保密审查时要讲）

- 映射规模：6 项目名 / 104+ 工号 / 89 人名 / 107 供应商，全文替换约 26 万处
- 第 1 轮发现：`信号架次有效性`等技术列存"0号机/X号机"型号引用、`arrangement_sources.raw_json` 嵌评审人工号、件号列嵌供应商名——均已入全文替换清单
- 第 2 轮发现（脚本缺陷，已修）：非 6 位短账号（"158"＝厂号账号）曾进入全文工号正则，可能误伤技术编号→限定 6 位工号
- 第 3 轮发现（设计教训，已修）：文件名 6 位号兜底替换曾把信号编号（420001＝ATA42 序列）误注册为工号→文件名 token 改独立 F 序号映射，不并入工号表
- 残留验证：367 探针全库 TEXT 列扫描，目标零命中（`docs/脱敏残留清单.md`）

## 风险与需要项目方的事（同方案第九节）

1. Comac Claw 接入规范（8/15 前）——未到不影响本周交付
2. 脱敏口径确认：`etl/anonymize_config.yaml` 送保密审查
3. 演示主线默认 X号机(45)+0号机(43)

## 下步（本周内）

- 残留零确认 → 图谱 v0 + stats → 20 条样题
- 图谱适配导出（Neo4j CSV）样例
