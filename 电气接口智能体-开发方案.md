# 电动飞机全机电气接口智能协同与追溯智能体 — 开发方案

版本：v1.0（2026-08-12）
执行人：Claude（开发）＋ 你（数据源、外部协调、口径确认）
数据基座：`D:\Downloads\MBSE综合管理平台`（EICD 综合管理平台，eicd.db 186MB，49 表）
交付仓库：`D:\Downloads\IPT`

---

## 〇、一页摘要

**交付承诺**
- 8/25：场景智能体（含平台接入包）＋ 3 个 Skill ＋ 知识图谱 v1（本体＋约 8 万节点/15 万关系）＋ 评测报告 v1
- 9/25：10 个 Skill ＋ 知识图谱终版 ＋ 2 个 MCP ＋ 评测集与跑分工具 ＋ 全套 schema/调用/验证材料

**三条执行原则**
1. **平台中立先行**：所有产物先做成平台中立格式（图谱 = jsonl 属性图，Skill = 标准化技能包，工具 = MCP 协议），Comac Claw 的接入规范到手后只写一层适配器。规范晚到不停线。
2. **确定性优先**：图谱由数据库确定性生成（ETL 可重跑），Skill 内核复用平台已验证代码，评测金标由 SQL 现算。没有一处依赖"抽取碰运气"。
3. **每周交付物是文件**：每周五硬盘上必须多出可打开、可运行、可核对的东西，清单见第八节。

---

## 一、交付物总表

| # | 交付物 | 形态 | 截止 |
|---|---|---|---|
| 1 | 领域本体（1 套） | `ontology/ontology.yaml` ＋ 说明文档 ＋ 图示 | 8/16 |
| 2 | 知识图谱（1 套） | `graph/nodes.jsonl` ＋ `graph/edges.jsonl` ＋ 统计报告；含 Claw 格式适配输出 | v1 8/25，终版 9/25 |
| 3 | 脱敏管线 | `etl/anonymize.py` ＋ 映射表 ＋ 脱敏库 `data/eicd_anon.db` | 8/16 |
| 4 | 场景智能体 | 平台中立参考实现（可本地运行）＋ Claw 接入包 | 8/25 |
| 5 | Skill × 3（首批） | `skills/S03链路追踪` `S04变更影响` `S08质量体检`，每个含 SKILL.md/提示词/工具契约/验证样例 | 8/25 |
| 6 | Skill × 10（全量） | 上述格式 × 10 | 9/25 |
| 7 | MCP × 2 | `mcp/eicd-graph`（查询）＋ `mcp/eicd-analysis`（分析），MCP stdio 协议 | graph 8/25，analysis 9/6 |
| 8 | 评测体系 | 生成器 ＋ 金标集（自动 150–200 条＋人工 20–30 条）＋ 跑分脚本 ＋ 报告 | v1 8/25，终版 9/25 |
| 9 | 验收材料包 | schema 文件、节点/关系统计、Skill—图谱对应表、调用链说明、验证记录 | 9/25 |

---

## 二、仓库布局（第 1 周建立，此后不变）

```
D:\Downloads\IPT\
├── data/                  # eicd.db 只读快照 + eicd_anon.db 脱敏库（均 .gitignore）
├── ontology/              # ontology.yaml + ontology.md（类/关系定义与说明）
├── etl/                   # anonymize.py / build_graph.py / refresh.md（刷新流程）
├── graph/                 # nodes.jsonl / edges.jsonl / stats.md / adapters/（claw、neo4j、rdf 导出）
├── mcp/
│   ├── eicd-graph/        # 查询类 MCP server（Python，直读脱敏库）
│   └── eicd-analysis/     # 分析类 MCP server（链路/影响/QA）
├── skills/
│   ├── S03-链路追踪/      # SKILL.md + prompt.md + tools.json + examples/ + eval/
│   ├── S04-变更影响/
│   ├── S08-质量体检/
│   └── ...（共 10 个，编号见第五节）
├── agent/
│   ├── reference/         # 平台中立参考实现（CLI/本地 Web，复用智能查询助手架构）
│   └── claw-adapter/      # Claw 接入层（规范到手后填充）
├── eval/                  # generate_eval.py / gold/ / score.py / reports/
└── docs/                  # 本方案、周报、演示脚本、验收自查清单
```

原则：**绝不写回 `MBSE综合管理平台` 目录**；eicd.db 复制快照后只读使用；复用其代码采取"拷贝＋改造入 IPT"方式，注明来源文件。

---

## 三、数据工程

### 3.1 数据源与快照策略
- 源：`MBSE综合管理平台/data/sqlite/eicd.db`（当前本地即有，186MB、49 表）。
- 快照两次：**8/15 冻结**用于 8/25 交付；**9/15 刷新**用于终版。ETL 可重跑，刷新成本≈分钟级。
- 默认纳入项目：X号机（project_id=45）＋ 0号机（43）两个构型；其余 4 个项目数据保留在图谱中但不作为演示主线。〔你可改选〕

### 3.2 脱敏（默认保守口径，不等审批先开工）
`etl/anonymize.py`：确定性假名化，输出 `eicd_anon.db` ＋ 映射表（留档不交付）。
- 替换：型号/项目名（X号机→A型验证机…）、员工姓名与 EID（→P001…）、供应商名称、users 表账号信息、IP/路径类字段。
- 保留：ATA 章节、设备/连接器/针孔技术编码、信号特性、连接关系、型谱代号（JONHON 件号规则若认定敏感则一并映射，开关可配）。
- 待你确认的口径以**配置文件**形式存在（`etl/anonymize_config.yaml`），口径变化只改配置重跑，不改代码。

### 3.3 图谱生成
`etl/build_graph.py`：脱敏库 → 按本体 schema 输出属性图。
- `nodes.jsonl`：`{id, type, label, props{...}, source_table, source_id}`——每个节点带回溯键，证据可定位到库表行。
- `edges.jsonl`：`{src, dst, type, props{...}}`。
- `stats.md`：分类型节点/关系计数（验收表直接引用）。
- `graph/adapters/`：Neo4j CSV 与 RDF/Turtle 导出器随手附上；**Claw 格式导出器**在规范到手后 1–2 天内补（纯格式转换）。

预计规模（按当前库真实行数）：节点 ≈ 80,000（设备 1,851／连接器 4,539／针孔 41,778／信号 18,445／导线 1,906／互联点·线束·型谱·系统·区域等），关系 ≈ 150,000（信号边 23,864／端点 40,270／贯通对 3,433／组成·穿越·供电·接地·变更关联等）。

---

## 四、本体（三层 18 类，翻译自已运行的 schema）

**产品定义层**：型号项目 / 区域(EDZ·分区) / ATA系统(章-节两级) / 设备 / 连接器 / 针孔 / 信号 / 信号分组 / 导线 / 线束 / 互联点(分离面·接地桩·端子排·死接头) / 连接器型谱 / 型谱针位
**规则层**：触件规格 / 供电电源代码 / 接地类别（规范文档以"规则文档"节点挂接）
**过程层**：变更事件 / 审批事件 / 人员角色（假名）

关系约 28 类：属于系统 / 安装于区域 / 拥有连接器 / 包含针孔 / 信号起于·止于 / 构成信号边 / 由导线实现 / 穿越互联点 / 归属线束 / 采用型谱 / 针位标准触件 / 供电来自 / 接地至 / 余度伙伴 / 变更于 / 审批于 / 负责人是 ……

交付：`ontology/ontology.yaml`（机器可读，ETL 与 MCP 共用）＋ `ontology/ontology.md`（人读，含 mermaid 图）。

---

## 五、10 个 Skill 规格

技能包统一格式：`SKILL.md`（用途/触发/输入/输出）＋ `prompt.md` ＋ `tools.json`（调用的 MCP 工具契约）＋ `examples/`（≥3 组输入输出验证样例）＋ `eval/`（≥10 道评测题）。

| # | Skill | 输入→输出 | 内核来源 | 周期 |
|---|---|---|---|---|
| S03 | 电气链路追踪 | 信号/设备 → 端到端物理路径（设备→针孔→导线→分离面→…），含供电与接地路径、证据链 | 导电网并查集（`relink-wire-signals.py`/`check-eicd-wire-gaps.py` 算法移植） | **8/25** |
| S04 | 变更影响分析 | 拟变更实体 → 受影响信号/导线/线束/型谱占用清单＋责任人（假名）＋建议流程 | 图谱邻域遍历＋change_logs 统计 | **8/25** |
| S08 | 数据质量体检 | 项目/系统/范围 → 分级诊断报告（连通缺口/端接尺寸/型谱占用/余度/接地） | 6 个 check-* 脚本改造为库调用 | **8/25** |
| S01 | 电气定义智能问答 | 自然语言 → 实体检索、组成、占用、归属答案＋出处 | 19 个查询工具 MCP 化 | 9/1 周 |
| S02 | 规范与规则问答 | 自然语言 → 命名规则/审批规则/分组规则解答＋条款出处 | docs/references 规则文档索引 | 9/1 周 |
| S05 | 故障排查辅助 | 故障现象(信号失效/设备失电) → 沿链路候选故障点清单＋该链路已知隐患叠加＋排查单 | S03＋S08 组合＋供电代码 | 9/8 周 |
| S06 | 接地网络分析 | 设备/区域 → 接地路径、接地类别匹配、悬空排查 | `ground_net.py` 语义层直用 | 9/8 周 |
| S07 | 构型对比 | 两个型号＋范围 → 设备/信号/接口差异清单 | 跨 project_id 对比查询 | 9/15 周 |
| S09 | 设计完整性检查 | 项目/区域 → EICD 边无导线兑现清单、针位冲突、命名合规 | wire-gaps＋occupancy＋命名规则 | 9/15 周 |
| S10 | 报告与协调单生成 | 前述任意结果 → 周报/接口协调单/评审数据包(md+html) | 模板引擎＋S01–S09 输出汇总 | 9/22 周 |

**8/25 演示故事线**（docs/演示脚本.md，第 3 周写）："X号机某配电连接器拟换型谱——S03 查清承载链路 → S04 看波及面与该通知谁 → S08 体检确认无新增隐患"，三个 Skill 串成一件完整的事。

---

## 六、MCP 设计

**`mcp/eicd-graph`**（8/25 交付，≈12 个只读工具）：list_projects / search_devices / get_device_detail / search_connectors / get_connector_detail / search_pins / get_pin_connections / search_signals / get_signal_detail / search_wires / get_harness_summary / trace_signal_path / get_change_history / run_readonly_sql（沙箱：只读连接＋语句门禁，沿用平台 sql-sandbox 思路）

**`mcp/eicd-analysis`**（9/6 交付，≈6 个工具）：impact_analysis / run_qa_check(种类可选) / qa_summary / ground_net_check / compare_configurations / generate_report

实现：Python，直读 `eicd_anon.db`。优先用 `mcp` 官方 SDK；**若环境装不上包，手写 stdio JSON-RPC 实现**（协议简单，半天工作量）——此项不依赖网络。

---

## 七、评测体系

- `eval/generate_eval.py`：按 Skill 分组的题目模板 × 随机采样实体 → 题目＋**SQL 现算金标**。自动集 150–200 条。
- 人工难题 20–30 条（多跳链路/故障传播/跨系统），〔需你或总体人员出题，9/12 前；缺席则自动集独立支撑验收〕。
- `eval/score.py`：查/检类按答案匹配打分（目标 ≥85%，预计 90%+）；析/报类按关键证据召回率。输出 `eval/reports/评测报告-日期.md`。
- 评测脚本随包交付，验收方可自行重跑。

---

## 八、逐周计划（每周交付物均不依赖外部输入即可产出）

### W1：8/12–8/16　地基
我做：仓库脚手架；eicd.db 快照；`anonymize.py`＋配置＋脱敏库；`ontology.yaml/.md`；`build_graph.py`；图谱 v0＋`stats.md`；评测生成器骨架。
**周五交付**：可重跑的脱敏＋ETL 管线、本体两件套、图谱 v0（真实节点/关系统计）。
你做：向平台方索取 Claw 接入规范；把脱敏配置送保密口径确认；确认演示主线型号。
兜底：三项你办的事本周都不到位 → 不影响本周任何交付物。

### W2：8/17–8/23　三个 Skill ＋ 智能体成形
我做：`eicd-graph` MCP v1；S03/S04/S08 技能包（含内核算法移植、样例、评测题）；参考实现智能体跑通（CLI＋本地 Web，复用智能查询助手架构）；自动评测集 v1（≥100 条）＋首次跑分；Claw 规范若已到 → 适配器＋图谱 Claw 格式导出。
**周五交付**：3 个完整技能包、MCP v1、可演示的本地智能体、评测报告 v1。
你做：Claw 联调环境/账号（8/20 前最好）；提供 LLM 接入方式（内网模型地址或 API key）。
兜底：Claw 环境不到位 → 交付"平台中立包＋接入映射文档"，适配器留 2 天工作量随到随接；LLM 不到位 → 三个 Skill 的调用链本身是确定性管道，可脱离 LLM 演示（脚本化端到端跑通＋录制样例），对话壳后补。

### W3：8/24–8/30　⭐ 8/25 里程碑 ＋ 扩展启动
8/24–25 我做：接入联调（或提交平台中立包）、演示脚本、阶段材料打包（图谱统计/Skill 清单/评测记录/验证样例）。
8/26–30 我做：S01/S02 开工（19 工具 MCP 化补全、规则文档索引库）。
**周五交付**：8/25 阶段包归档＋S01/S02 半成品（可运行的工具层）。

### W4：8/31–9/6
我做：S01/S02 完成；`eicd-analysis` MCP 交付；评测集扩至 150。
**周五交付**：5 个 Skill 累计、第二个 MCP、评测报告 v2。

### W5：9/7–9/13
我做：S05 故障排查（S03+S08 组合链）＋ S06 接地网络（ground_net 直用）；人工难题（若收到）入金标。
**周五交付**：7 个 Skill 累计、故障排查演示样例。

### W6：9/14–9/20
我做：S07 构型对比＋S09 完整性检查；9/15 数据快照刷新→图谱终版重建；全量评测回归（目标 200 条）。
**周五交付**：9 个 Skill 累计、图谱终版、评测报告 v3。

### W7：9/21–9/25　⭐ 终验
我做：S10 报告生成；端到端调用链检查；验收材料包（schema/节点关系统计/Skill—图谱对应表/调用说明/验证记录）；发布。
**9/25 交付**：全部 9 项交付物齐套。

---

## 九、需要你补齐的事项（每项都有"缺席不停线"预案）

| # | 事项 | 建议时限 | 缺席时的兜底 |
|---|---|---|---|
| 1 | **Comac Claw 接入规范**（Skill 格式/图谱接入格式/是否支持 MCP/部署方式） | 8/15 | 全线按平台中立格式开发；交"接入映射文档"，规范到手后 1–2 天补适配器 |
| 2 | **脱敏口径确认**（anonymize_config.yaml 过保密审查） | 8/15 | 先按最保守口径（多脱不少脱）开发；口径放宽只改配置重跑 |
| 3 | **Claw 联调环境/账号** | 8/20 | 8/25 交平台中立包＋本地演示录屏；联调顺延不影响 Skill/图谱验收 |
| 4 | **LLM 接入方式**（内网模型 base_url＋key，或允许用的外部 API） | 8/20 | Skill 以确定性调用链演示；对话层随模型到位即接（适配层现成，改 env 即换） |
| 5 | 演示主线确认（默认 X号机＋0号机；演示故事线认可） | 8/22 | 按默认执行 |
| 6 | 人工评测难题 20–30 条（总体人员出题） | 9/12 | 自动金标集 150–200 条独立支撑 85% 验收口径 |
| 7 | 9/15 前若服务器库有大更新，跑一次 pull-db 给我新快照 | 9/15 | 沿用 8/15 快照交付，注明数据截止日 |

---

## 十、风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| Claw 规范与中立格式差异大（如要求特定图数据库/特定 DSL） | 中 | 中立格式信息是全集，任何目标格式都是投影；adapters/ 已预留 Neo4j/RDF 两种示例路径 |
| 脱敏口径收得极紧（连 ATA/技术编码都要脱） | 低 | 配置化替换可扩展到任意列；极端情况下用"结构保真+全编码假名"模式，图谱结构价值不损 |
| 导电网算法移植后与原脚本结果不一致 | 低 | 移植时以原脚本输出为金标做对拍测试（同库同参数结果逐行一致才算完成） |
| 评测 85% 不达标 | 低 | 查/检类金标 SQL 可算，失分即工具 bug，可修；口径文件把"析/报类"绑定证据召回率而非答案准确率 |
| 我方时间被其他任务挤占 | — | 每周交付物独立成立，进度按周可盘点；周报落 `docs/周报/` |

---

## 十一、里程碑自查清单

**8/25 交付即勾选**：
- [ ] 智能体可运行（Claw 上或本地参考实现＋接入包）
- [ ] S03/S04/S08 技能包齐套（说明/提示词/工具契约/≥3 样例/≥10 评测题）
- [ ] 图谱 v1：nodes/edges 文件＋stats.md（真实节点/关系数）＋本体两件套
- [ ] eicd-graph MCP 可调用
- [ ] 评测报告 v1（自动集 ≥100 条，含分数）
- [ ] 演示故事线可完整走通（录屏留档）

**9/25 交付即勾选**：
- [ ] 10 个技能包齐套＋Skill—图谱节点/关系对应表
- [ ] 图谱终版（9/15 数据）＋schema＋统计
- [ ] 两个 MCP＋调用链说明
- [ ] 评测集（自动＋人工）＋跑分脚本＋评测报告 v3
- [ ] 验收材料包归档＋发布记录
